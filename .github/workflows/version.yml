name: Check Docker Version

on:
  - push
  - pull_request

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Check Docker Version
        run: docker version

      - name: Environment
        run: env
